web browers are software programs used to access the internet, web languages like HTML, CSS, and Java Script are used to translate from browsers into layouts of texts, graohics and so mich more.
DOM- document object model. interface to a document and stipulates how that doc is accessed and changed by defining it's structure ona logical way.
HTML standard language to create web pages, XHTML is a family to xml. XML mark up language and file format for storing, transmitting and reconstructing arbitrary data.
headings, paragraphs, lists, links.
main/homepage located at top level.
best naming practices: camel casing, under scores and dashes.
